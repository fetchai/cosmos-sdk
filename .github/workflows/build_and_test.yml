name: Build and test
on:
  - pull_request
  - push
jobs:
  build-and-test:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - name: Set up ssh keys
        uses: webfactory/ssh-agent@v0.2.0
        with:
          ssh-private-key: ${{ secrets.FETCH_BOT_SSH_KEY }}
      - name: Build Docker container
        env:
          DOCKER_BUILDKIT: 1
        run: |
          docker build \
            --no-cache \
            --progress plain \
            --ssh default \
            --tag fetch_cosmos_sdk:$GITHUB_SHA \
            --file ./Dockerfile_fetch_ci \
            ./
      - name: Run make test
        run: |
          docker run --rm fetch_cosmos_sdk:$GITHUB_SHA \
            make test
      - name: Run make test-ledger-mock
        run: |
          docker run --rm fetch_cosmos_sdk:$GITHUB_SHA \
            make test-ledger-mock
